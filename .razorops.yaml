tasks:
  build-image:
    steps:
    - checkout
    - docker/build:
        image: registry.digitalocean.com/test-demo
        tags: ["${CI_COMMIT_SHA:0:8}", "latest", "${CI_REPO_BRANCH}"]
        push: true